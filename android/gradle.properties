# Default build settings for react-native-smart-otp. Consuming apps override
# these via their own gradle.properties (rootProject.ext) when needed.
SmartOtp_compileSdkVersion=34
SmartOtp_targetSdkVersion=34
SmartOtp_minSdkVersion=21
